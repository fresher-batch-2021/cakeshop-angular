import { Addproductdatatype } from './addproductdatatype';

describe('Addproductdatatype', () => {
  it('should create an instance', () => {
    expect(new Addproductdatatype()).toBeTruthy();
  });
});
